export function addition(x, y) {
  return x + y;
}

export function substract(x, y) {
  return x - y;
}

// CommonJs
// ExpressJs
// module.exports = { addition, substract };

// ESM
// Vue Nuxt
// export default addition;
